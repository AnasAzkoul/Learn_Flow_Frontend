---
name: debugging-with-nuxt-devtools
description: TODO — placeholder. See stacks/vue/README.md for the intended scope of this skill.
when_to_activate: Not yet — this skill is a placeholder and not ready for activation.
status: stub
---

# debugging-with-nuxt-devtools

> **Status: stub.** This skill is in the index but not yet written. The intended scope is captured in `stacks/vue/README.md` under the Tier 1 skill table.
>
> **Why a stub instead of nothing:** the directory shape is part of the playbook contract — projects that sync this stack with `--with-nuxt` get the full set of folder names, so adding content later is a content-only change, not a structure change.
>
> **To fill in:** open an MR following the §15 living-doc proposal flow. Quote the originating session's §11.5 proposal, justify the convention as cross-project (not just one team's preference), and link to the real incident or pattern that triggered the skill.

## Defensive default (in case this stub gets activated anyway)

This skill is a stub — no team-specific guidance yet. Defer to the Tier 1 CLAUDE.md block's investigation-order section: server vs client first, then Nuxt DevTools panels (Pages → Components → Imports → Server Routes), then drop into the Tier 0 order.

## Activation

This stub will not be activated by Claude Code at session start because the `status: stub` frontmatter signals it's not ready. Once the content lands, the frontmatter switches to a real `when_to_activate` description.
